cache:
  directories:
    - node_modules
language: node_js
dist: trusty
sudo: false
node_js:
  - '7'
install:
  - npm install
services:
  - docker
script:
  - npm test
after_script:
  - npm install -g codeclimate-test-reporter
  - codeclimate-test-reporter < ./coverage/lcov.info
after_success:
  # - chmod +x ./bin/ecs-deploy.sh
  # - ./bin/ecs-deploy.sh
